# Weather App

## Variables de entorno

Crea un archivo `.env.local` en la raiz del proyecto con:

```env
OPENWEATHER_API_KEY=tu_api_key
```

Nota: actualmente la ruta interna del proyecto tambien acepta `WEATHER_API_KEY`, pero la variable recomendada es `OPENWEATHER_API_KEY`.
